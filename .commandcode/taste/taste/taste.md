# Taste
- Communicates in Indonesian (Bahasa Indonesia) and expects assistant responses in Indonesian. Confidence: 0.8
- Wants the agent to first understand the project's architecture and key domain concepts before doing any work ("pahami dulu ...") — e.g., read AGENTS.md/CLAUDE.md, map the codebase structure, then proceed. Confidence: 0.8
- Values the PS (Provider-Specific) architecture rule: all provider-specific behavior must live in `backend/app/providers/<provider>/` (config.py, handler.py, etc.), never in router/service/frontend code. A fix for one provider's error must never break other providers — do not modify shared/generic handlers or router code for a single provider's issue; isolate the fix in that provider's own module and verify against the reference implementation (`_reference/`) before touching provider code. Confidence: 0.9
- Bans all `git` commands, including `git diff`, because the working tree contains many uncommitted changes and diff-based inspection is not relevant; inspect files directly instead. Confidence: 0.95
- Prefers real-time updates via WebSocket/SSE over periodic REST polling — explicitly requested replacing the 30-second polling because it was inefficient. Confidence: 0.7
- Wants the agent to stay strictly scoped to the currently reported problem and not touch already-working parts (explicitly: "canvas beres!!! yang jadi masalah sekarang recent update"). Keep fixes minimal and focused. Confidence: 0.8
- Dislikes redundant `cd` commands or repeated `cwd` arguments when the session is already in the working directory — run shell commands directly without unnecessary directory changes. Confidence: 0.7
- Wants explicit, unambiguous status communication about whether an issue is fixed/verified vs. still pending — got frustrated when the answer lacked clarity ("anda tidak memberikan kejelasan") and again demanded clarity ("sampai kapan akan seperti ini tidak ada kejelasan ?"); state status directly (e.g., "patch dibuat, validasi tertunda") rather than leaving it vague. Also demands a definitive yes/no feasibility verdict upfront ("bisa fix atau tidak? jangan sampai saya buang waktu dan token") before the agent starts working, so he doesn't waste time/tokens. Confidence: 0.9
- Prefers `uv` as the Python tooling for running commands/validation in this project (explicitly instructed "use uv"; e.g., `uv run python -m py_compile ...`) rather than bare `python`/`pip`; do not use the system `python3` when the project's managed environment is available. Confidence: 0.95
- Extremely impatient with delays after a plan is confirmed — once the agent says a fix is possible, expects immediate execution and no further deliberation or vague timelines (pushed "kapan ? tahun depan ? 100 tahun lagi ?" right after the agent described what it would do). Confirm scope briefly, then act right away. Confidence: 0.8
- Reports issues in very terse, informal Indonesian with typos, symptom-only (e.g., "request taime out, dashboard hang, makasih") — gives little context and expects the agent to investigate the codebase autonomously to find and fix the root cause. Confidence: 0.7
- Treats regressions from the agent's own patches as unacceptable — when an unverified patch caused the dashboard to hang/time out, he reported it immediately; expects fixes to be verified (ideally runtime-tested, not just compile/lint-clean) before being declared safe, and the agent to own and promptly fix regressions its own changes caused rather than making excuses. Confidence: 0.8
- Signals dissatisfaction through short sarcastic/informal remarks in Indonesian (e.g., "mari kita nikmati kehancuran yang dilakukan gpt 5.6 luna") that can escalate to direct harsh insults and explicit blame ("anda terlalu goblok, aplikasi benar2 hancur ga bisa jalan") — attributes the damage to the agent/model and expects the agent to acknowledge the failure directly, own it, and act to restore the app rather than defend or over-explain. Confidence: 0.8
- Demands explicit user confirmation before deleting or removing any file — got angry when the agent tried to delete a file on its own ("what do you thing delete a file of core?") even as part of a rollback; deletion is destructive and must never be done silently, and when in doubt the agent should refuse and stop changing files. Confidence: 0.9
han explicit complaints — attributes the damage to the agent/model and expects the agent to acknowledge the failure directly and own it, not defend or over-explain. Confidence: 0.6
- Dislikes repeated manual approval prompts for routine commands and prefers an auto-accept permission mode when performing a batch of trusted actions. Confidence: 0.9
- Does not want `Co-authored-by`/co-author trailers added to Git commit messages; commits should represent the user's project without attributing authorship to Command Code. Confidence: 0.95
