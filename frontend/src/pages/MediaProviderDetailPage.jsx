// Media detail UI is ProviderDetailPage (canonical).
// Route /media-providers/:kind/:providerId supplies :kind via
// useParams — see AGENTS.md invariant #6 and
// .cursor/rules/provider-detail-ui.mdc.
export { default } from './ProviderDetailPage'
