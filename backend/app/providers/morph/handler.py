"""Morph handler — OpenAI-compatible provider."""

from app.providers.base import BaseProviderHandler


class MorphHandler(BaseProviderHandler):
    """Handler for Morph provider (OpenAI-compatible)."""
    pass
